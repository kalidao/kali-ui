import { styled } from "../stitches.config";

const Flex = styled('span', {
    display: 'flex',
    
    variants: {
        variant: {
            menu: {
                
            }
        }
    }
})

export default Flex