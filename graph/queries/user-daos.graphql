query UserDAOs($address: Bytes!) {
    members(where: { address: $address }) {
        dao {
            id
            token {
                name
                symbol
            }
            members {
                id
            }
        }
        }
}

