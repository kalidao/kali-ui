import React from 'react'
import { Box } from '../../styles/elements'
import CLUB_ABI from "../../abi/KaliClubSig.json";

export default function Profile({ club }) {
  
  return (
    <Box css={{
        position: 'relative',
        top: '1rem',
        left: '5rem',
        border: '1px solid $gray800',
        padding: '1rem'
    }}>

    </Box>
  )
}
